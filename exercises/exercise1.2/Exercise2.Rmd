
### Exercise 1
Tasks:

1. Ui.R : define a plotOutput() of the "plotDensity"
2. Server.R : generate plot of standard normal distribution and put it into
output slot "plotDensity"

### Exercise 2
Tasks:

1. Ui.R : add widget for selecting mean of the normal distribution with numericInput() and assigning it slot "meanValue"
2. Server.R : add "input$meanValue" as parameter for plotting